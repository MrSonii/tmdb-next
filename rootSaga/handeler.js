// import { put, call } from "redux-saga/effects";

// import requestGetUser from "../api/getData";
// import { getDataFail, getDataSucc } from "../store/action";

export function* handleGetUser(action) {
  // try {
  //   const response = yield call(requestGetUser);
  //   const { data } = response;
  //   yield put(getDataSucc(data));
  // } catch (error) {
  //   yield put(getDataFail(error));
  // }
}
